import Vue from "vue";
import Vuex from "vuex";
import { FieldStore } from "@/store/field";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    FieldStore
  },
});
